using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class escabar : MonoBehaviour
{
    public AudioSource sonidodeEscabar;
    // Start is called before the first frame update
    void Start()
    {
       sonidodeEscabar.Play();
    }
}
